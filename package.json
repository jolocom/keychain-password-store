{
  "name": "@jolocom/keychain-password-store",
  "version": "0.1.0",
  "main": "js/index.js",
  "author": "Jolocom",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf js && yarn tsc -p .",
    "clean": "rm -rf node_modules",
    "prepare": "yarn build"
  },
  "devDependencies": {
    "@jolocom/sdk": "^0.8.0",
    "typescript": "^3.9.3"
  },
  "dependencies": {
    "react-native-keychain": "^6.0.0"
  }
}
